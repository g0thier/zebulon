---
type Props = {
  label?: string;
  variant?: "status" | "section";
};

const { label, variant = "status" } = Astro.props;
---

{label && <span class:list={["status-badge", `status-badge--${variant}`]}>{label}</span>}
